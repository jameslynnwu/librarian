# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Synthesize long audio resources
    description: Synthesize long audio a resource
    examples: |-
      To synthesize long audio the resource, run:

          $ {command}
  arguments:
    params:
      - help_text: |-
          The name of the voice. If both the name and the gender are not set,
          the service will choose a voice based on the other parameters such as
          language_code.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          collection: texttospeech.
          disable_auto_completers: false
        required: true
      - arg_name: parent
        api_field: synthesizeLongAudioRequest.parent
        help_text: |-
          The resource states of the request in the form of
          `projects/*/locations/*`.
        is_positional: false
        required: false
      - arg_name: input-text
        api_field: synthesizeLongAudioRequest.input.text
        help_text: The raw text to be synthesized.
        is_positional: false
        required: false
      - arg_name: input-markup
        api_field: synthesizeLongAudioRequest.input.markup
        help_text: |-
          Markup for Chirp 3: HD voices specifically. This field may not be used
          with any other voices.
        is_positional: false
        required: false
      - arg_name: input-ssml
        api_field: synthesizeLongAudioRequest.input.ssml
        help_text: |-
          The SSML document to be synthesized. The SSML document must be valid
          and well-formed. Otherwise the RPC will fail and return
          [google.rpc.Code.INVALID_ARGUMENT][google.rpc.Code.INVALID_ARGUMENT]. For
          more information, see
          [SSML](https://cloud.google.com/text-to-speech/docs/ssml).
        is_positional: false
        required: false
      - arg_name: input-multi-speaker-markup
        api_field: synthesizeLongAudioRequest.input.multiSpeakerMarkup
        help_text: |-
          The multi-speaker input to be synthesized. Only applicable for
          multi-speaker synthesis.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: input-prompt
        api_field: synthesizeLongAudioRequest.input.prompt
        help_text: |-
          This system instruction is supported only for controllable/promptable voice
          models. If this system instruction is used, we pass the unedited text to
          Gemini-TTS. Otherwise, a default system instruction is used. AI Studio
          calls this system instruction, Style Instructions.
        is_positional: false
        required: false
      - arg_name: input-custom-pronunciations
        api_field: synthesizeLongAudioRequest.input.customPronunciations
        help_text: |-
          The pronunciation customizations are applied to the input. If
          this is set, the input is synthesized using the given pronunciation
          customizations.

          The initial support is for en-us, with plans to expand to other locales in
          the future. Instant Clone voices aren't supported.

          In order to customize the pronunciation of a phrase, there must be an exact
          match of the phrase in the input types. If using SSML, the phrase must not
          be inside a phoneme tag.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: audio-config-audio-encoding
        api_field: synthesizeLongAudioRequest.audioConfig.audioEncoding
        help_text: The format of the audio byte stream.
        is_positional: false
        required: true
        choices:
          - arg_value: linear-16
            enum_value: LINEAR16
            help_text: |-
              Uncompressed 16-bit signed little-endian samples (Linear PCM).
              Audio content returned as LINEAR16 also contains a WAV header.
          - arg_value: mp-3
            enum_value: MP3
            help_text: MP3 audio at 32kbps.
          - arg_value: ogg-opus
            enum_value: OGG_OPUS
            help_text: |-
              Opus encoded audio wrapped in an ogg container. The result is a
              file which can be played natively on Android, and in browsers (at least
              Chrome and Firefox). The quality of the encoding is considerably higher
              than MP3 while using approximately the same bitrate.
          - arg_value: mulaw
            enum_value: MULAW
            help_text: |-
              8-bit samples that compand 14-bit audio samples using G.711 PCMU/mu-law.
              Audio content returned as MULAW also contains a WAV header.
          - arg_value: alaw
            enum_value: ALAW
            help_text: |-
              8-bit samples that compand 14-bit audio samples using G.711 PCMU/A-law.
              Audio content returned as ALAW also contains a WAV header.
          - arg_value: pcm
            enum_value: PCM
            help_text: |-
              Uncompressed 16-bit signed little-endian samples (Linear PCM).
              Note that as opposed to LINEAR16, audio won't be wrapped in a WAV (or
              any other) header.
          - arg_value: m-4-a
            enum_value: M4A
            help_text: M4A audio.
      - arg_name: audio-config-speaking-rate
        api_field: synthesizeLongAudioRequest.audioConfig.speakingRate
        help_text: |-
          Input only. Speaking rate/speed, in the range [0.25, 2.0]. 1.0 is
          the normal native speed supported by the specific voice. 2.0 is twice as
          fast, and 0.5 is half as fast. If unset(0.0), defaults to the native 1.0
          speed. Any other values < 0.25 or > 2.0 will return an error.
        is_positional: false
        required: false
        type: float
      - arg_name: audio-config-pitch
        api_field: synthesizeLongAudioRequest.audioConfig.pitch
        help_text: |-
          Input only. Speaking pitch, in the range [-20.0, 20.0]. 20 means
          increase 20 semitones from the original pitch. -20 means decrease 20
          semitones from the original pitch.
        is_positional: false
        required: false
        type: float
      - arg_name: audio-config-volume-gain-db
        api_field: synthesizeLongAudioRequest.audioConfig.volumeGainDb
        help_text: |-
          Input only. Volume gain (in dB) of the normal native volume
          supported by the specific voice, in the range [-96.0, 16.0]. If unset, or
          set to a value of 0.0 (dB), will play at normal native signal amplitude. A
          value of -6.0 (dB) will play at approximately half the amplitude of the
          normal native signal amplitude. A value of +6.0 (dB) will play at
          approximately twice the amplitude of the normal native signal amplitude.
          Strongly recommend not to exceed +10 (dB) as there's usually no effective
          increase in loudness for any value greater than that.
        is_positional: false
        required: false
        type: float
      - arg_name: audio-config-sample-rate-hertz
        api_field: synthesizeLongAudioRequest.audioConfig.sampleRateHertz
        help_text: |-
          The synthesis sample rate (in hertz) for this audio. When this is
          specified in SynthesizeSpeechRequest, if this is different from the voice's
          natural sample rate, then the synthesizer will honor this request by
          converting to the desired sample rate (which might result in worse audio
          quality), unless the specified sample rate is not supported for the
          encoding chosen, in which case it will fail the request and return
          [google.rpc.Code.INVALID_ARGUMENT][google.rpc.Code.INVALID_ARGUMENT].
        is_positional: false
        required: false
        type: int
      - arg_name: audio-config-effects-profile-id
        api_field: synthesizeLongAudioRequest.audioConfig.effectsProfileId
        help_text: |-
          Input only. An identifier which selects 'audio effects' profiles
          that are applied on (post synthesized) text to speech. Effects are applied
          on top of each other in the order they are given. See
          [audio
          profiles](https://cloud.google.com/text-to-speech/docs/audio-profiles) for
          current supported profile ids.
        is_positional: false
        required: false
        repeated: true
      - arg_name: output-gcs-uri
        api_field: synthesizeLongAudioRequest.outputGcsUri
        help_text: |-
          Specifies a Cloud Storage URI for the synthesis results. Must be
          specified in the format: `gs://bucket_name/object_name`, and the bucket
          must already exist.
        is_positional: false
        required: true
      - arg_name: voice-language-code
        api_field: synthesizeLongAudioRequest.voice.languageCode
        help_text: |-
          The language (and potentially also the region) of the voice
          expressed as a [BCP-47](https://www.rfc-editor.org/rfc/bcp/bcp47.txt)
          language tag, e.g. "en-US". This should not include a script tag (e.g. use
          "cmn-cn" rather than "cmn-Hant-cn"), because the script will be inferred
          from the input provided in the SynthesisInput.  The TTS service
          will use this parameter to help choose an appropriate voice.  Note that
          the TTS service may choose a voice with a slightly different language code
          than the one selected; it may substitute a different region
          (e.g. using en-US rather than en-CA if there isn't a Canadian voice
          available), or even a different language, e.g. using "nb" (Norwegian
          Bokmal) instead of "no" (Norwegian)".
        is_positional: false
        required: true
      - arg_name: voice-ssml-gender
        api_field: synthesizeLongAudioRequest.voice.ssmlGender
        help_text: |-
          The preferred gender of the voice. If not set, the service will
          choose a voice based on the other parameters such as language_code and
          name. Note that this is only a preference, not requirement; if a
          voice of the appropriate gender is not available, the synthesizer should
          substitute a voice with a different gender rather than failing the request.
        is_positional: false
        required: false
        choices:
          - arg_value: male
            enum_value: MALE
            help_text: A male voice.
          - arg_value: female
            enum_value: FEMALE
            help_text: A female voice.
          - arg_value: neutral
            enum_value: NEUTRAL
            help_text: A gender-neutral voice. This voice is not yet supported.
      - arg_name: voice-custom-voice
        api_field: synthesizeLongAudioRequest.voice.customVoice
        help_text: |-
          The configuration for a custom voice. If [CustomVoiceParams.model] is set,
          the service will choose the custom voice matching the specified
          configuration.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: voice-voice-clone
        api_field: synthesizeLongAudioRequest.voice.voiceClone
        help_text: |-
          The configuration for a voice clone. If
          [VoiceCloneParams.voice_clone_key] is set, the service chooses the voice
          clone matching the specified configuration.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: voice-model-name
        api_field: synthesizeLongAudioRequest.voice.modelName
        help_text: |-
          The name of the model. If set, the service will choose the model
          matching the specified configuration.
        is_positional: false
        required: false
      - arg_name: voice-multi-speaker-voice-config
        api_field: synthesizeLongAudioRequest.voice.multiSpeakerVoiceConfig
        help_text: |-
          The configuration for a Gemini multi-speaker text-to-speech
          setup. Enables the use of two distinct voices in a single synthesis
          request.
        is_positional: false
        required: false
        type: arg_object
  request:
    api_version: v1
    collection:
      - texttospeech.projects.locations
    method: synthesizeLongAudio
  async:
    collection:
      - texttospeech.projects.operations
    extract_resource_result: false
