# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Update profiles
    description: Update a profile
    examples: |-
      To update the profile, run:

          $ {command}
  arguments:
    params:
      - help_text: Output only. Opaque, server-assigned, unique ID for this profile.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          attributes:
            - attribute_name: project
              help: The project id of the {resource} resource.
              parameter_name: projectsId
              property: core/project
            - attribute_name: profile
              help: The profile id of the {resource} resource.
              parameter_name: profilesId
          collection: cloudprofiler.projects.profiles
          disable_auto_completers: false
          name: profile
          plural_name: profiles
        required: true
      - arg_name: profile-type
        api_field: profile.profileType
        help_text: |-
          Type of profile.
          For offline mode, this must be specified when creating the profile. For
          online mode it is assigned and returned by the server.
        is_positional: false
        required: false
        choices:
          - arg_value: cpu
            enum_value: CPU
            help_text: Thread CPU time sampling.
          - arg_value: wall
            enum_value: WALL
            help_text: Wallclock time sampling. More expensive as stops all threads.
          - arg_value: heap
            enum_value: HEAP
            help_text: |-
              In-use heap profile. Represents a snapshot of the allocations that are
              live at the time of the profiling.
          - arg_value: threads
            enum_value: THREADS
            help_text: Single-shot collection of all thread stacks.
          - arg_value: contention
            enum_value: CONTENTION
            help_text: Synchronization contention profile.
          - arg_value: peak-heap
            enum_value: PEAK_HEAP
            help_text: Peak heap profile.
          - arg_value: heap-alloc
            enum_value: HEAP_ALLOC
            help_text: |-
              Heap allocation profile. It represents the aggregation of all allocations
              made over the duration of the profile. All allocations are included,
              including those that might have been freed by the end of the profiling
              interval. The profile is in particular useful for garbage collecting
              languages to understand which parts of the code create most of the garbage
              collection pressure to see if those can be optimized.
      - arg_name: deployment
        api_field: profile.deployment
        help_text: Deployment this profile corresponds to.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: duration
        api_field: profile.duration
        help_text: |-
          Duration of the profiling session.
          Input (for the offline mode) or output (for the online mode).
          The field represents requested profiling duration. It may slightly differ
          from the effective profiling duration, which is recorded in the profile
          data, in case the profiling can't be stopped immediately (e.g. in case
          stopping the profiling is handled asynchronously).
        is_positional: false
        required: false
        type: arg_object
      - arg_name: profile-bytes
        api_field: profile.profileBytes
        help_text: |-
          Input only. Profile bytes, as a gzip compressed serialized proto, the
          format is https://github.com/google/pprof/blob/master/proto/profile.proto.
        is_positional: false
        required: false
        type: bytes
      - arg_name: labels
        api_field: profile.labels
        help_text: |-
          Input only. Labels associated to this specific profile. These labels will
          get merged with the deployment labels for the final data set. See
          documentation on deployment labels for validation rules and limits.
        is_positional: false
        required: false
        repeated: true
        clearable: true
        spec:
          - api_field: key
          - api_field: value
  request:
    api_version: v2
    collection:
      - cloudprofiler.projects.profiles
    static_fields:
      updateMask: '*'
  update:
    disable_auto_field_mask: true
    read_modify_update: true
