# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Write resources
    description: Write a resource
    examples: |-
      To write the resource, run:

          $ {command}
  arguments:
    params:
      - help_text: |-
          The parent catalog resource name, such as
          `projects/1234/locations/global/catalogs/default_catalog`.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          collection: retail.
          disable_auto_completers: false
        required: true
      - arg_name: event-type
        api_field: userEvent.eventType
        help_text: |-
          User event type. Allowed values are:

          * `add-to-cart`: Products being added to cart.
          * `remove-from-cart`: Products being removed from cart.
          * `category-page-view`: Special pages such as sale or promotion pages
            viewed.
          * `detail-page-view`: Products detail page viewed.
          * `home-page-view`: Homepage viewed.
          * `purchase-complete`: User finishing a purchase.
          * `search`: Product search.
          * `shopping-cart-page-view`: User viewing a shopping cart.
        is_positional: false
        required: true
      - arg_name: visitor-id
        api_field: userEvent.visitorId
        help_text: |-
          A unique identifier for tracking visitors.

          For example, this could be implemented with an HTTP cookie, which should be
          able to uniquely identify a visitor on a single device. This unique
          identifier should not change if the visitor log in/out of the website.

          Don't set the field to the same fixed ID for different users. This mixes
          the event history of those users together, which results in degraded model
          quality.

          The field must be a UTF-8 encoded string with a length limit of 128
          characters. Otherwise, an INVALID_ARGUMENT error is returned.

          The field should not contain PII or user-data. We recommend to use Google
          Analytics [Client
          ID](https://developers.google.com/analytics/devguides/collection/analyticsjs/field-reference#clientId)
          for this field.
        is_positional: false
        required: true
      - arg_name: session-id
        api_field: userEvent.sessionId
        help_text: |-
          A unique identifier for tracking a visitor session with a length limit of
          128 bytes. A session is an aggregation of an end user behavior in a time
          span.

          A general guideline to populate the session_id:
          1. If user has no activity for 30 min, a new session_id should be assigned.
          2. The session_id should be unique across users, suggest use uuid or add
          visitor_id as prefix.
        is_positional: false
        required: false
      - arg_name: event-time
        api_field: userEvent.eventTime
        help_text: |-
          Only required for
          [UserEventService.ImportUserEvents][google.cloud.retail.v2.UserEventService.ImportUserEvents]
          method. Timestamp of when the user event happened.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: experiment-ids
        api_field: userEvent.experimentIds
        help_text: |-
          A list of identifiers for the independent experiment groups this user event
          belongs to. This is used to distinguish between user events associated with
          different experiment setups (e.g. using Retail API, using different
          recommendation models).
        is_positional: false
        required: false
        repeated: true
      - arg_name: attribution-token
        api_field: userEvent.attributionToken
        help_text: |-
          Highly recommended for user events that are the result of
          [PredictionService.Predict][google.cloud.retail.v2.PredictionService.Predict].
          This field enables accurate attribution of recommendation model
          performance.

          The value must be a valid
          [PredictResponse.attribution_token][google.cloud.retail.v2.PredictResponse.attribution_token]
          for user events that are the result of
          [PredictionService.Predict][google.cloud.retail.v2.PredictionService.Predict].
          The value must be a valid
          [SearchResponse.attribution_token][google.cloud.retail.v2.SearchResponse.attribution_token]
          for user events that are the result of
          [SearchService.Search][google.cloud.retail.v2.SearchService.Search].

          This token enables us to accurately attribute page view or purchase back to
          the event and the particular predict response containing this
          clicked/purchased product. If user clicks on product K in the
          recommendation results, pass
          [PredictResponse.attribution_token][google.cloud.retail.v2.PredictResponse.attribution_token]
          as a URL parameter to product K's page. When recording events on product
          K's page, log the
          [PredictResponse.attribution_token][google.cloud.retail.v2.PredictResponse.attribution_token]
          to this field.
        is_positional: false
        required: false
      - arg_name: product-details
        api_field: userEvent.productDetails
        help_text: |-
          The main product details related to the event.

          This field is optional except for the following event types:

          * `add-to-cart`
          * `detail-page-view`
          * `purchase-complete`

          In a `search` event, this field represents the products returned to the end
          user on the current page (the end user may have not finished browsing the
          whole page yet). When a new page is returned to the end user, after
          pagination/filtering/ordering even for the same query, a new `search` event
          with different
          [product_details][google.cloud.retail.v2.UserEvent.product_details] is
          desired. The end user may have not finished browsing the whole page yet.
        is_positional: false
        required: false
        repeated: true
        type: arg_object
      - arg_name: completion-detail
        api_field: userEvent.completionDetail
        help_text: |-
          The main auto-completion details related to the event.

          This field should be set for `search` event when autocomplete function is
          enabled and the user clicks a suggestion for search.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: attributes
        api_field: userEvent.attributes
        help_text: |-
          Extra user event features to include in the recommendation model.

          If you provide custom attributes for ingested user events, also include
          them in the user events that you associate with prediction requests. Custom
          attribute formatting must be consistent between imported events and events
          provided with prediction requests. This lets the Retail API use
          those custom attributes when training models and serving predictions, which
          helps improve recommendation quality.

          This field needs to pass all below criteria, otherwise an INVALID_ARGUMENT
          error is returned:

          * The key must be a UTF-8 encoded string with a length limit of 5,000
            characters.
          * For text attributes, at most 400 values are allowed. Empty values are not
            allowed. Each value must be a UTF-8 encoded string with a length limit of
            256 characters.
          * For number attributes, at most 400 values are allowed.

          For product recommendations, an example of extra user information is
          traffic_channel, which is how a user arrives at the site. Users can arrive
          at the site by coming to the site directly, coming through Google
          search, or in other ways.
        is_positional: false
        required: false
        repeated: true
        spec:
          - api_field: key
          - api_field: value
      - arg_name: cart-id
        api_field: userEvent.cartId
        help_text: |-
          The ID or name of the associated shopping cart. This ID is used
          to associate multiple items added or present in the cart before purchase.

          This can only be set for `add-to-cart`, `purchase-complete`, or
          `shopping-cart-page-view` events.
        is_positional: false
        required: false
      - arg_name: purchase-transaction
        api_field: userEvent.purchaseTransaction
        help_text: |-
          A transaction represents the entire purchase transaction.

          Required for `purchase-complete` events. Other event types should not set
          this field. Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: search-query
        api_field: userEvent.searchQuery
        help_text: |-
          The user's search query.

          See [SearchRequest.query][google.cloud.retail.v2.SearchRequest.query] for
          definition.

          The value must be a UTF-8 encoded string with a length limit of 5,000
          characters. Otherwise, an INVALID_ARGUMENT error is returned.

          At least one of
          [search_query][google.cloud.retail.v2.UserEvent.search_query] or
          [page_categories][google.cloud.retail.v2.UserEvent.page_categories] is
          required for `search` events. Other event types should not set this field.
          Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
      - arg_name: filter
        api_field: userEvent.filter
        help_text: |-
          The filter syntax consists of an expression language for constructing a
          predicate from one or more fields of the products being filtered.

          See [SearchRequest.filter][google.cloud.retail.v2.SearchRequest.filter] for
          definition and syntax.

          The value must be a UTF-8 encoded string with a length limit of 1,000
          characters. Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
      - arg_name: order-by
        api_field: userEvent.orderBy
        help_text: |-
          The order in which products are returned.

          See [SearchRequest.order_by][google.cloud.retail.v2.SearchRequest.order_by]
          for definition and syntax.

          The value must be a UTF-8 encoded string with a length limit of 1,000
          characters. Otherwise, an INVALID_ARGUMENT error is returned.

          This can only be set for `search` events. Other event types should not set
          this field. Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
      - arg_name: offset
        api_field: userEvent.offset
        help_text: |-
          An integer that specifies the current offset for pagination (the 0-indexed
          starting location, amongst the products deemed by the API as relevant).

          See [SearchRequest.offset][google.cloud.retail.v2.SearchRequest.offset] for
          definition.

          If this field is negative, an INVALID_ARGUMENT is returned.

          This can only be set for `search` events. Other event types should not set
          this field. Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
        type: int
      - arg_name: page-categories
        api_field: userEvent.pageCategories
        help_text: |-
          The categories associated with a category page.

          To represent full path of category, use '>' sign to separate different
          hierarchies. If '>' is part of the category name, replace it with
          other character(s).

          Category pages include special pages such as sales or promotions. For
          instance, a special sale page may have the category hierarchy:
          "pageCategories" : ["Sales > 2017 Black Friday Deals"].

          Required for `category-page-view` events. At least one of
          [search_query][google.cloud.retail.v2.UserEvent.search_query] or
          [page_categories][google.cloud.retail.v2.UserEvent.page_categories] is
          required for `search` events. Other event types should not set this field.
          Otherwise, an INVALID_ARGUMENT error is returned.
        is_positional: false
        required: false
        repeated: true
      - arg_name: user-info
        api_field: userEvent.userInfo
        help_text: User information.
        is_positional: false
        required: false
        type: arg_object
      - arg_name: uri
        api_field: userEvent.uri
        help_text: |-
          Complete URL (window.location.href) of the user's current page.

          When using the client side event reporting with JavaScript pixel and Google
          Tag Manager, this value is filled in automatically. Maximum length 5,000
          characters.
        is_positional: false
        required: false
      - arg_name: referrer-uri
        api_field: userEvent.referrerUri
        help_text: |-
          The referrer URL of the current page.

          When using the client side event reporting with JavaScript pixel and Google
          Tag Manager, this value is filled in automatically.
        is_positional: false
        required: false
      - arg_name: page-view-id
        api_field: userEvent.pageViewId
        help_text: |-
          A unique ID of a web page view.

          This should be kept the same for all user events triggered from the same
          pageview. For example, an item detail page view could trigger multiple
          events as the user is browsing the page. The `pageViewId` property should
          be kept the same for all these events so that they can be grouped together
          properly.

          When using the client side event reporting with JavaScript pixel and Google
          Tag Manager, this value is filled in automatically.
        is_positional: false
        required: false
      - arg_name: entity
        api_field: userEvent.entity
        help_text: |-
          The entity for customers that may run multiple different entities, domains,
          sites or regions, for example, `Google US`, `Google Ads`, `Waymo`,
          `google.com`, `youtube.com`, etc.
          We recommend that you set this field to get better per-entity search,
          completion, and prediction results.
        is_positional: false
        required: false
      - arg_name: write-async
        api_field: writeAsync
        help_text: |-
          If set to true, the user event will be written asynchronously after
          validation, and the API will respond without waiting for the write.
          Therefore, silent failures can occur even if the API returns success. In
          case of silent failures, error messages can be found in Stackdriver logs.
        action: store_true
        is_positional: false
        required: false
        type: bool
        default: null
  request:
    api_version: v2
    collection:
      - retail.projects.locations.catalogs.userEvents
    method: write
