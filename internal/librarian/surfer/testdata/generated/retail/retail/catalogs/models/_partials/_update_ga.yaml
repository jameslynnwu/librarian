# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Update models
    description: Update a model
    examples: |-
      To update the model, run:

          $ {command}
  arguments:
    params:
      - help_text: |-
          The fully qualified resource name of the model.

          Format:
          `projects/{project_number}/locations/{location_id}/catalogs/{catalog_id}/models/{model_id}`
          catalog_id has char limit of 50.
          recommendation_model_id has char limit of 40.
        is_positional: true
        is_primary_resource: true
        resource_spec:
          attributes:
            - attribute_name: project
              help: The project id of the {resource} resource.
              parameter_name: projectsId
              property: core/project
            - attribute_name: location
              help: The location id of the {resource} resource.
              parameter_name: locationsId
            - attribute_name: catalog
              help: The catalog id of the {resource} resource.
              parameter_name: catalogsId
            - attribute_name: model
              help: The model id of the {resource} resource.
              parameter_name: modelsId
          collection: retail.projects.locations.catalogs.models
          disable_auto_completers: false
          name: model
          plural_name: models
        required: true
      - arg_name: display-name
        api_field: model.displayName
        help_text: |-
          The display name of the model.

          Should be human readable, used to display Recommendation Models in the
          Retail Cloud Console Dashboard. UTF-8 encoded string with limit of 1024
          characters.
        is_positional: false
        required: true
      - arg_name: training-state
        api_field: model.trainingState
        help_text: |-
          The training state that the model is in (e.g.
          `TRAINING` or `PAUSED`).

          Since part of the cost of running the service
          is frequency of training - this can be used to determine when to train
          model in order to control cost. If not specified: the default value for
          `CreateModel` method is `TRAINING`. The default value for
          `UpdateModel` method is to keep the state the same as before.
        is_positional: false
        required: false
        choices:
          - arg_value: paused
            enum_value: PAUSED
            help_text: The model training is paused.
          - arg_value: training
            enum_value: TRAINING
            help_text: The model is training.
      - arg_name: type
        api_field: model.type
        help_text: |-
          The type of model e.g. `home-page`.

          Currently supported values: `recommended-for-you`, `others-you-may-like`,
          `frequently-bought-together`, `page-optimization`, `similar-items`,
          `buy-it-again`, `on-sale-items`, and `recently-viewed`(readonly value).

          This field together with
          [optimization_objective][google.cloud.retail.v2.Model.optimization_objective]
          describe model metadata to use to control model training and serving.
          See https://cloud.google.com/retail/docs/models
          for more details on what the model metadata control and which combination
          of parameters are valid. For invalid combinations of parameters (e.g. type
          = `frequently-bought-together` and optimization_objective = `ctr`), you
          receive an error 400 if you try to create/update a recommendation with
          this set of knobs.
        is_positional: false
        required: true
      - arg_name: optimization-objective
        api_field: model.optimizationObjective
        help_text: |-
          The optimization objective e.g. `cvr`.

          Currently supported
          values: `ctr`, `cvr`, `revenue-per-order`.

           If not specified, we choose default based on model type.
          Default depends on type of recommendation:

          `recommended-for-you` => `ctr`

          `others-you-may-like` => `ctr`

          `frequently-bought-together` => `revenue_per_order`

          This field together with
          [optimization_objective][google.cloud.retail.v2.Model.type]
          describe model metadata to use to control model training and serving.
          See https://cloud.google.com/retail/docs/models
          for more details on what the model metadata control and which combination
          of parameters are valid. For invalid combinations of parameters (e.g. type
          = `frequently-bought-together` and optimization_objective = `ctr`), you
          receive an error 400 if you try to create/update a recommendation with
          this set of knobs.
        is_positional: false
        required: false
      - arg_name: periodic-tuning-state
        api_field: model.periodicTuningState
        help_text: |-
          The state of periodic tuning.

          The period we use is 3 months - to do a
          one-off tune earlier use the `TuneModel` method. Default value
          is `PERIODIC_TUNING_ENABLED`.
        is_positional: false
        required: false
        choices:
          - arg_value: periodic-tuning-disabled
            enum_value: PERIODIC_TUNING_DISABLED
            help_text: |-
              The model has periodic tuning disabled. Tuning
              can be reenabled by calling the `EnableModelPeriodicTuning`
              method or by calling the `TuneModel` method.
          - arg_value: all-tuning-disabled
            enum_value: ALL_TUNING_DISABLED
            help_text: |-
              The model cannot be tuned with periodic tuning OR the
              `TuneModel` method. Hide the options in customer UI and
              reject any requests through the backend self serve API.
          - arg_value: periodic-tuning-enabled
            enum_value: PERIODIC_TUNING_ENABLED
            help_text: |-
              The model has periodic tuning enabled. Tuning
              can be disabled by calling the `DisableModelPeriodicTuning`
              method.
      - arg_name: filtering-option
        api_field: model.filteringOption
        help_text: |-
          If `RECOMMENDATIONS_FILTERING_ENABLED`, recommendation filtering
          by attributes is enabled for the model.
        is_positional: false
        required: false
        choices:
          - arg_value: recommendations-filtering-disabled
            enum_value: RECOMMENDATIONS_FILTERING_DISABLED
            help_text: Recommendation filtering is disabled.
          - arg_value: recommendations-filtering-enabled
            enum_value: RECOMMENDATIONS_FILTERING_ENABLED
            help_text: Recommendation filtering is enabled.
      - arg_name: model-features-config
        api_field: model.modelFeaturesConfig
        help_text: Additional model features config.
        is_positional: false
        required: false
        type: arg_object
  request:
    api_version: v2
    collection:
      - retail.projects.locations.catalogs.models
    static_fields:
      updateMask: '*'
  update:
    disable_auto_field_mask: true
    read_modify_update: true
