# -*- coding: utf-8 -*- #
# Copyright 2026 Google LLC. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# NOTE: This file is autogenerated and should not be edited by hand.
- release_tracks:
    - GA
  auto_generated: true
  help_text:
    brief: Allocate quota resources
    description: Allocate quota a resource
    examples: |-
      To allocate quota the resource, run:

          $ {command}
  arguments:
    params:
      - arg_name: service-name
        api_field: allocateQuotaRequest.serviceName
        help_text: |-
          Name of the service as specified in the service configuration. For example,
          `"pubsub.googleapis.com"`.

          See [google.api.Service][google.api.Service] for the definition of a service name.
        is_positional: false
        required: false
      - arg_name: allocate-operation-operation-id
        api_field: allocateQuotaRequest.allocateOperation.operationId
        help_text: |-
          Identity of the operation. This is expected to be unique within the scope
          of the service that generated the operation, and guarantees idempotency in
          case of retries.

          In order to ensure best performance and latency in the Quota backends,
          operation_ids are optimally associated with time, so that related
          operations can be accessed fast in storage. For this reason, the
          recommended token for services that intend to operate at a high QPS is
          Unix time in nanos + UUID
        is_positional: false
        required: false
      - arg_name: allocate-operation-method-name
        api_field: allocateQuotaRequest.allocateOperation.methodName
        help_text: |-
          Fully qualified name of the API method for which this quota operation is
          requested. This name is used for matching quota rules or metric rules and
          billing status rules defined in service configuration.

          This field should not be set if any of the following is true:
          (1) the quota operation is performed on non-API resources.
          (2) quota_metrics is set because the caller is doing quota override.


          Example of an RPC method name:
              google.example.library.v1.LibraryService.CreateShelf
        is_positional: false
        required: false
      - arg_name: allocate-operation-consumer-id
        api_field: allocateQuotaRequest.allocateOperation.consumerId
        help_text: |-
          Identity of the consumer for whom this quota operation is being performed.

          This can be in one of the following formats:
            project:<project_id>,
            project_number:<project_number>,
            api_key:<api_key>.
        is_positional: false
        required: false
      - arg_name: allocate-operation-labels
        api_field: allocateQuotaRequest.allocateOperation.labels
        help_text: Labels describing the operation.
        is_positional: false
        required: false
        repeated: true
        spec:
          - api_field: key
          - api_field: value
      - arg_name: allocate-operation-quota-metrics
        api_field: allocateQuotaRequest.allocateOperation.quotaMetrics
        help_text: |-
          Represents information about this operation. Each MetricValueSet
          corresponds to a metric defined in the service configuration.
          The data type used in the MetricValueSet must agree with
          the data type specified in the metric definition.

          Within a single operation, it is not allowed to have more than one
          MetricValue instances that have the same metric names and identical
          label value combinations. If a request has such duplicated MetricValue
          instances, the entire request is rejected with
          an invalid argument error.

          This field is mutually exclusive with method_name.
        is_positional: false
        required: false
        repeated: true
        type: arg_object
      - arg_name: allocate-operation-quota-mode
        api_field: allocateQuotaRequest.allocateOperation.quotaMode
        help_text: Quota mode for this operation.
        is_positional: false
        required: false
        choices:
          - arg_value: unspecified
            enum_value: UNSPECIFIED
            help_text: Guard against implicit default. Must not be used.
          - arg_value: normal
            enum_value: NORMAL
            help_text: |-
              For AllocateQuota request, allocates quota for the amount specified in
              the service configuration or specified using the quota metrics. If the
              amount is higher than the available quota, allocation error will be
              returned and no quota will be allocated.
              If multiple quotas are part of the request, and one fails, none of the
              quotas are allocated or released.
          - arg_value: best-effort
            enum_value: BEST_EFFORT
            help_text: |-
              The operation allocates quota for the amount specified in the service
              configuration or specified using the quota metrics. If the amount is
              higher than the available quota, request does not fail but all available
              quota will be allocated.
              For rate quota, BEST_EFFORT will continue to deduct from other groups
              even if one does not have enough quota. For allocation, it will find the
              minimum available amount across all groups and deduct that amount from
              all the affected groups.
          - arg_value: check-only
            enum_value: CHECK_ONLY
            help_text: |-
              For AllocateQuota request, only checks if there is enough quota
              available and does not change the available quota. No lock is placed on
              the available quota either.
          - arg_value: query-only
            enum_value: QUERY_ONLY
            help_text: |-
              Unimplemented. When used in AllocateQuotaRequest, this returns the
              effective quota limit(s) in the response, and no quota check will be
              performed. Not supported for other requests, and even for
              AllocateQuotaRequest, this is currently supported only for allowlisted
              services.
          - arg_value: adjust-only
            enum_value: ADJUST_ONLY
            help_text: |-
              The operation allocates quota for the amount specified in the service
              configuration or specified using the quota metrics. If the requested
              amount is higher than the available quota, request does not fail and
              remaining quota would become negative (going over the limit).
              Not supported for Rate Quota.
      - arg_name: service-config-id
        api_field: allocateQuotaRequest.serviceConfigId
        help_text: |-
          Specifies which version of service configuration should be used to process
          the request. If unspecified or no matching version can be found, the latest
          one will be used.
        is_positional: false
        required: false
  request:
    api_version: v1
    collection:
      - servicecontrol.services
    method: allocateQuota
